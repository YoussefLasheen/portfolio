import 'package:flutter/material.dart';

const Gradient mainGradient = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    stops: [
      0.05,
      0.95,
    ],
    colors: [
      Color.fromRGBO(22, 0, 64, 1),
      Color.fromRGBO(121, 1, 140, 1),
    ]);

